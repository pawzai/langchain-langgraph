# AI Production Issue Investigator — pinned dependencies
# Verified on Python 3.14.6 / Windows. LangChain + LangGraph are on the v1 API line.

# --- AI building blocks ---
langchain==1.3.18
langchain-core==1.6.1
langchain-text-splitters==1.1.2

# --- Local model runtime integration ---
langchain-ollama==1.1.0
ollama==0.6.2

# --- Workflow orchestration ---
langgraph==1.2.11
langgraph-checkpoint-sqlite==3.1.1

# --- Vector store ---
langchain-chroma==1.1.0
chromadb==1.5.9

# --- Retrieval: hybrid search, reranking and multi-format ingestion ---
# langchain-community supplies BM25Retriever, PyPDFLoader and the cross-encoder wrapper.
langchain-community==0.4.1
rank-bm25==0.2.2
pypdf==6.1.1
# Pulls torch. Install the CPU-only build to avoid a multi-gigabyte CUDA download:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
sentence-transformers==5.1.2

# --- Backend ---
fastapi==0.141.1
uvicorn[standard]==0.52.4

# --- UI ---
streamlit==1.62.0

# --- Support ---
pydantic==2.13.5
pydantic-settings==2.15.0
requests==2.34.2
PyYAML==6.0.3
